# AI Test Runner 评分与权重分配 FAQ

## 1. `calculateNormalizedWeights` 的设计逻辑是什么？

该函数负责将一组混合了“显式分值/置信度”与“未定义项”的权重列表，统一转换为总和为 1.0（或独立缩放）的置信度列表。其核心策略是**“保障性分配”**：
- **量纲统一**：自动识别小于 1 的值为置信度，大于 1 的值按 `maxScore` 缩放。
- **低保预算**：通过 `totalUnassignedWeight` 为所有未定义项预留总预算。
- **空间挤占**：如果显式权重过高导致剩余空间不足，则优先保障“低保预算”，同比压缩显式权重。

## 2. 为什么将 `unassignedWeight` 重命名为 `totalUnassignedWeight`？

原名容易被误解为“每个未定义项的权重”。新名称强调这是一个**总预算（Budget）**。无论有多少个未定义项，它们将平分这一块“蛋糕”，这使得在处理变长数组或对象属性时，总分的确定性更高。

## 3. `normalize: true` 与 `false` 有什么区别？

- **`normalize: true` (默认)**：对应 AND 逻辑（`weightedSumStrategy`）。所有项的权重总和严格等于 1.0。
- **`normalize: false`**：对应 OR 逻辑（`maxStrategy`）。每一项保持其独立的置信度，不强制总和归一，适用于“取最大值”的评分场景。

## 4. 为什么 `MatchResult.score` 始终是 0-1 的置信度？

为了保证递归计算的数学简洁性。
- **子节点**：只负责报告“我完成了百分之几（0.0 - 1.0）”。
- **父节点**：负责将子节点的置信度乘以其权重比例（Weight），汇总得到父节点的置信度。
这种“比例 × 比例”的计算方式使得无论嵌套多少层，逻辑都不会因为绝对分值的变化而崩溃。

## 5. `allocatedScore` 的作用是什么？

`allocatedScore` 代表分配给当前节点的**绝对分值预算**（默认为 100）。
- 虽然叶子节点（如 `validateString`）主要返回置信度，但 `allocatedScore` 会在递归过程中不断被权重切割并向下传递。
- **用途**：提供给复杂算子（如 `$sequence`）或自定义算子，用于计算绝对扣分值，或在最终报告中展示该项“值多少分”。

## 6. 为什么之前测试中会出现“验证失败（Pass: false）但分数为 1”的情况？

这是由于 `src/validate/diff.ts` 中的一个逻辑漏洞：
- `validateStringDiff` 在开始时将 `score` 初始化为 `1.0`。
- 在简单的字符串比对路径中，当发现不匹配并设 `pass = false` 时，忘记将 `score` 重置为 `0`。
- **修复方案**：确保所有失败路径都显式地将 `score` 归零。

## 7. `autoConfidence` 选项的作用？

为了灵活性，默认开启。它会自动将 `[0, 1]` 之间的显式权重视为已经归一化的置信度。如果用户确实需要定义一个极小的原始分（如总分 100，某项仅占 0.5 分），可以设置 `autoConfidence: false` 来强制进行 `w / maxScore` 的缩放。
