# 评分系统修订历史记录

## 2026-03-25 (第三次修订：元数据架构重构)

### 1. 修订背景

在早期的实现中，`ai-test-runner` 采用了一种“隐式元数据”设计。`score`, `title`, `critical` 等键名被硬编码为保留字，并在对象校验过程中被强行过滤。这导致了一个严重的**核心冲突**：当用户的业务数据中包含同名键（例如校验一个包含 `score` 字段的 JSON 对象）时，这些字段会被引擎误认为是验证规则的元数据而忽略，导致无法对这些业务数据进行正确匹配。这种“带内信令（In-band Signaling）”的设计混淆了**控制平面（验证逻辑）**与**数据平面（被验证数据）**。

### 2. 核心改动点

#### A. 引入 `$meta` 容器与 `$` 前缀简写

本着“显式优于隐式，控制权归还用户”的原则，我们实施了一次彻底的 **Breaking Change**：

* **显式模式 (Explicit Mode)**：引入 `$meta` 容器。只要存在 `$meta` 对象，该层级的所有元数据必须定义在其中。
* **简写模式 (Shorthand Mode)**：提供 `$score`, `$title` 等以 `$` 开头的简写键。
* **释放业务空间**：`score`, `title`, `description` 等非 `$` 开头的键名现在被视为 100% 的业务数据，不再具有任何特殊含义。

#### B. 互斥原则 (Exclusivity Principle)

为了消除“当两者并存时谁覆盖谁”的模糊性，我们确立了严格的互斥逻辑：

* **如果对象中定义了 `$meta`**：则同级的 `$` 前缀简写键将失效，回归为业务数据。这为用户提供了一个除了 `$meta` 和已注册算子外完全干净的顶级命名空间。
* **如果对象中未定义 `$meta`**：则尝试提取 `$score` 等简写键作为元数据。

### 3. 技术实现细节

#### A. 统一提取入口 (SRP)

* **Refactor**: 将原本散落在 `core.ts` 和各个算子（`$and`, `$or` 等）中的权重提取逻辑统一收敛至 `src/validate/utils.ts` 中的 `getScoreConfig` 函数。
* **Lesson**: 重复的逻辑是维护的噩梦，统一入口是确保行为一致性的唯一途径。

#### B. 区分“未分配”与“默认值”

* **Logic**: `getScoreConfig` 现在返回 `weight: number | null`。
  * **null**: 表示用户未定义评分，触发系统的“动态权重分配”逻辑。
  * **1**: 表示用户显式定义了权重为 1。
* **Impact**: 如果统一返回 1，将破坏 `calculateNormalizedWeights` 识别未标注项的能力，导致评分聚合结果与预期偏差。

#### C. 详情树层级对齐

在压力测试中发现，算子的“虚拟性（Virtual）”会影响标题（Title）挂载的层级。我们修正了断言逻辑，确认对于原子算子（如 `$contains`），标题应挂载在父级的属性节点上，而对于逻辑容器，标题则挂载在容器自身。

### 4. 验证情况

* **全量矩阵测试**: 建立了 `test/validate/object-metadata-breaking.test.ts`，覆盖了互斥性、业务键名释放、简写模式及算子共存等所有排列组合。
* **压力边界测试**: 建立了 `test/validate/object-metadata-stress.test.ts`，覆盖了深度递归互斥性、未知 `$` 键名处理及 `$meta` 类型鲁棒性。
* **回归测试**: 更新了 `test/scoring.test.ts`，确保所有现有功能平滑迁移至新语法。

---

## 2026-03-04 (第二次重构：路径自动化与架构纯净化)

### 1. 修订背景

在第一版评分系统上线后，发现路径追踪逻辑散落在各算子内部，导致代码冗余且极易在新增算子时引入路径断层。为了实现真正的 **KISS** 架构，我们将路径生成与元数据修补职能从算子层上移至核心引擎层。

### 2. 核心改动点

#### A. 术语与概念演进

* **虚拟容器 (Virtual Container)**：原 `transparent` 更名为 `virtual`。强调该算子仅作为逻辑组织者，在物理路径上不产生占位（穿透），其子项内容直接容纳在父级。
* **身份代表契约 (Identity Representation)**：规定每一层递归结果（MatchResult）必须由其 `details` 数组中的第一个节点代表其路径身份。

#### B. 架构职能单一化 (SRP)

* **策略层 (Strategy)**：回归本质，仅负责收集并展平子项返回的详情列表，不再尝试“猜测”或“修改”路径 Key。
* **核心出口修补 (Core Recursive Exit)**：统一在 `validate` 函数出口处调用 `patchMatchResult`。它是路径生成的唯一终审官，确保树结构的完整性。
* **修补逻辑智能优化**：
  * **物理层（有 Key）**：如果子项尚未代表该 Key，则进行包装（Wrap）。
  * **虚拟层（无 Key）**：绝不包装，直接渗透元数据。

#### C. 开发者体验 (KISS)

* **Context 助手**：`ValidationContext` 暴露了 `distribute()` 和 `aggregate()` 方法，算子编写者无需关心复杂的权重分配参数。
* **路径模板自动化与变量注入**：`createChildContext` 支持通过 `$([a-zA-Z]+)` 格式注入上下文变量。
  * `$key`: 当前元素的键名或索引。
  * `$index`: 数字索引（即使键名是字符串）。
  * `$count`: 容器内的总元素数量。
  * `$operator`: 当前执行的算子名称（如 `$and`）。
  * **默认策略**：虚拟算子默认使用 `$operator[$key]` 模板，在多元素场景下自动生成如 `$and[0]` 的可读路径。
* **算子主权**：算子可以在元数据中声明 `strategy`（如 `$or` 锁定 `max`），该策略优先于用户配置，确保逻辑语义不被篡改。

---

## 2026-03-03 (第一次修订：多维度与得分回溯能力)

### 1. 修订背景

为了支持更复杂的 AI 代理（Agent）评估需求，需要从单一的 Pass/Fail 验证转向多维度、可量化的评估体系。本次修订引入了多维度标签、直观扣分逻辑以及深层得分回溯能力。

### 2. 核心改动点

#### A. 数据结构增强

- **MatchResult**: 增加了 `title`, `dimension`, `critical` 和 `details` 字段。
- **AIScoreConfig**: 增加了对负数 `value` 的支持，以及 `dimension` 属性。
- **AITestLogItem**: 增加了 `scoreDetails` 用于存储完整的得分树。

#### B. 算法优化

- **calculateNormalizedWeights**:
  - 实现了正负分流逻辑。
  - 正数项（奖励）按 Balanced 模式瓜分 1.0 预算。
  - 负数项（惩罚）按相同 scale 独立计算，不参与预算瓜分。
- **weightedSumStrategy**:
  - 实现了负权重聚合。
  - 引入了**虚拟失败注入**逻辑：当 Critical 惩罚项匹配成功时，由于底层返回 pass: true，必须手动注入 failure 才能保证红线判定生效。

#### C. 路径系统对齐 (注：已在第二次重构中通过自动化方案取代)

- **算子虚拟化**: 将 `$and` 和 `$or` 设为虚拟（Virtual），但在其对应的 Handler 内部通过 `createChildContext` 自动化处理索引。

### 3. 核心业务逻辑锚点 (防误删/误改指南)

在后续维护中，请务必保留以下逻辑，除非有明确的架构重构计划：

1. **utils.ts / processValidationResult**:
    - **必须保留**: 即使 `pass: true` 也要提取 `details`、`dimension` 和 `title`。
    - **后果**: 移除会导致得分报告变为空白，多维度评分功能失效。

2. **strategies.ts / weightedSumStrategy**:
    - **必须保留**: 惩罚项匹配成功时的虚拟 failure 注入。
    - **后果**: 移除会导致标记为 `critical` 的负分项无法触发测试失败。

3. **core.ts / validate**:
    - **必须保留**: 递归出口处的 `patchMatchResult` 调用。
    - **后果**: 移除会导致路径系统崩溃，所有详情树将失去层级信息。

### 4. 验证情况

已通过全量测试，包括路径自动化验证（`test/validate/path-automation.test.ts`）和所有存量回归测试。
